# Core (required)
requests>=2.31
PyYAML>=6.0
openpyxl>=3.1

# Local web UI (`python3 main.py serve`)
fastapi>=0.110
uvicorn>=0.27
pydantic>=2.0

# Optional enrichment - the ground-truth path works without these.
# The LLM defaults to the Claude Code CLI (`claude -p`), which needs no package.
tavily-python>=0.7   ; extra == "search"
openai>=2.0          ; extra == "openrouter"
